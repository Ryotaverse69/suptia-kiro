#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Lightweight pre-commit checks only
echo "🚀 Running lightweight pre-commit checks..."

# Run lint-staged for changed files (format + lint only)
npx lint-staged

echo "✅ Pre-commit checks passed! Heavy checks will run in CI."
