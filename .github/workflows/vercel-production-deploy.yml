name: Vercel Production Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: vercel-production-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: '20'

jobs:
  deploy:
    name: Deploy to Vercel Production
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.0.0

      - name: Deploy to Vercel Production
        uses: amondnet/vercel-action@v20
        id: vercel-deploy
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod --confirm'
          working-directory: ./

      - name: Wait for Deployment to be Ready
        run: |
          echo "Deployment URL: ${{ steps.vercel-deploy.outputs.preview-url }}"
          echo "Waiting for deployment to be ready..."
          
          # より長い待機時間とリトライ機能
          MAX_ATTEMPTS=10
          ATTEMPT=1
          
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS - Waiting 30 seconds..."
            sleep 30
            
            if [ -n "${{ steps.vercel-deploy.outputs.preview-url }}" ]; then
              if curl -f -s "${{ steps.vercel-deploy.outputs.preview-url }}" > /dev/null 2>&1; then
                echo "✅ Deployment is ready!"
                break
              fi
            fi
            
            ATTEMPT=$((ATTEMPT + 1))
          done
          
          if [ $ATTEMPT -gt $MAX_ATTEMPTS ]; then
            echo "⚠️ Deployment readiness check timed out, but continuing..."
          fi

      - name: Install dependencies for verification
        run: |
          pnpm install

      - name: Verify Production Deployment
        id: verify-deployment
        env:
          DEPLOYMENT_URL: ${{ steps.vercel-deploy.outputs.preview-url }}
        run: |
          echo "🔍 Verifying deployment..."
          echo "Deployment URL: $DEPLOYMENT_URL"
          
          if [ -n "$DEPLOYMENT_URL" ]; then
            # より詳細な検証
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$DEPLOYMENT_URL" || echo "000")
            echo "HTTP Status: $HTTP_STATUS"
            
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ Deployment is accessible with HTTP 200"
              
              # Vercelヘッダーの確認
              VERCEL_ID=$(curl -s -I "$DEPLOYMENT_URL" | grep -i "x-vercel-id" | cut -d' ' -f2 | tr -d '\r\n' || echo "")
              if [ -n "$VERCEL_ID" ]; then
                echo "✅ Vercel deployment ID: $VERCEL_ID"
                echo "vercel_id=$VERCEL_ID" >> $GITHUB_OUTPUT
              fi
              
              echo "deployment_status=success" >> $GITHUB_OUTPUT
            else
              echo "❌ Deployment verification failed with HTTP $HTTP_STATUS"
              echo "deployment_status=failed" >> $GITHUB_OUTPUT
              exit 1
            fi
          else
            echo "❌ No deployment URL found"
            echo "deployment_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Advanced Deployment Monitoring
        id: monitor-deployment
        continue-on-error: true
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          echo "🔍 高度なデプロイ監視を実行中..."
          
          # 監視スクリプトの実行
          if [ -f "scripts/monitor-deployment.mjs" ]; then
            if node scripts/monitor-deployment.mjs; then
              echo "✅ デプロイ監視完了"
              echo "monitoring_status=success" >> $GITHUB_OUTPUT
            else
              echo "⚠️ デプロイ監視で問題を検出"
              echo "monitoring_status=warning" >> $GITHUB_OUTPUT
            fi
          else
            echo "⚠️ 監視スクリプトが見つかりません"
            echo "monitoring_status=skipped" >> $GITHUB_OUTPUT
          fi

      - name: Diagnose Domain Settings
        id: diagnose-domain
        continue-on-error: true
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          echo "🔍 Running domain settings diagnosis..."
          
          if node scripts/diagnose-domain-settings.mjs; then
            echo "✅ Domain diagnosis completed successfully"
            echo "diagnosis_status=success" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Domain diagnosis detected issues"
            echo "diagnosis_status=warning" >> $GITHUB_OUTPUT
          fi

      - name: Verify Custom Domain
        id: verify-domain
        continue-on-error: true
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          echo "🔍 Verifying custom domain configuration..."
          
          if node scripts/verify-custom-domain.mjs; then
            echo "✅ Custom domain verification successful"
            echo "domain_status=success" >> $GITHUB_OUTPUT
          else
            echo "❌ Custom domain verification failed"
            echo "domain_status=failed" >> $GITHUB_OUTPUT
          fi

      - name: Create Success Comment
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEPLOYMENT_URL: ${{ steps.vercel-deploy.outputs.preview-url }}
          DOMAIN_STATUS: ${{ steps.verify-domain.outputs.domain_status }}
          DIAGNOSIS_STATUS: ${{ steps.diagnose-domain.outputs.diagnosis_status }}
          VERCEL_ID: ${{ steps.verify-deployment.outputs.vercel_id }}
        run: |
          # 最新のPRにコメントを追加（もしあれば）
          LATEST_PR=$(gh pr list --state merged --limit 1 --json number --jq '.[0].number' || echo "")
          
          DOMAIN_EMOJI="${{ steps.verify-domain.outputs.domain_status == 'success' && '✅' || '⚠️' }}"
          DOMAIN_TEXT="${{ steps.verify-domain.outputs.domain_status == 'success' && 'Ready' || 'Issues Detected' }}"
          DIAGNOSIS_EMOJI="${{ steps.diagnose-domain.outputs.diagnosis_status == 'success' && '✅' || '⚠️' }}"
          
          cat > comment_body.txt << 'EOF'
          🚀 Production Deployment Successful
          
          Deployment Details:
          - 🔗 Deployment URL: $DEPLOYMENT_URL
          - 🆔 Vercel ID: `${VERCEL_ID:-'N/A'}`
          - 📝 Commit: `${{ github.sha }}`
          - 🌐 Domain Status: $DOMAIN_EMOJI $DOMAIN_TEXT
          - 🔍 Diagnosis: $DIAGNOSIS_EMOJI Domain configuration checked
          - ⏰ Deployed: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
          
          Production URLs:
          - 🌍 [suptia.com](https://suptia.com)
          - 🌍 [www.suptia.com](https://www.suptia.com)
          
          The production environment has been successfully updated!
          
          📋 Diagnostic Reports:
          - Domain diagnostic report available in workflow artifacts
          - Custom domain verification report available in workflow artifacts
          EOF
          
          COMMENT_BODY=$(cat comment_body.txt)
          
          if [ -n "$LATEST_PR" ]; then
            gh pr comment $LATEST_PR --body "$COMMENT_BODY" || echo "Could not add comment to PR"
          else
            echo "No recent PR found, skipping comment"
          fi
          
          # Deployment summary
          echo "## Production Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Status: ✅ Successful" >> $GITHUB_STEP_SUMMARY
          echo "- Deployment URL: $DEPLOYMENT_URL" >> $GITHUB_STEP_SUMMARY
          echo "- Domain Status: $DOMAIN_EMOJI $DOMAIN_TEXT" >> $GITHUB_STEP_SUMMARY
          echo "- Vercel ID: \`${VERCEL_ID:-'N/A'}\`" >> $GITHUB_STEP_SUMMARY

      - name: Auto Recovery Attempt
        if: failure()
        id: auto-recovery
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          echo "🔄 自動復旧を試行中..."
          
          # 自動復旧スクリプトの実行
          if [ -f "scripts/auto-recovery.mjs" ]; then
            if node scripts/auto-recovery.mjs; then
              echo "✅ 自動復旧が成功しました"
              echo "recovery_status=success" >> $GITHUB_OUTPUT
            else
              echo "❌ 自動復旧に失敗しました"
              echo "recovery_status=failed" >> $GITHUB_OUTPUT
            fi
          else
            echo "⚠️ 自動復旧スクリプトが見つかりません"
            echo "recovery_status=skipped" >> $GITHUB_OUTPUT
          fi

      - name: Notify Deployment Failure
        if: failure() && steps.auto-recovery.outputs.recovery_status != 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "❌ Production deployment failed and auto-recovery unsuccessful"
          
          # Create an issue for deployment failure
          gh issue create \
            --title "🚨 Production Deployment Failed - $(date '+%Y-%m-%d %H:%M')" \
            --body "Production deployment failed. Check workflow logs for details." \
            --label "deployment,production,urgent,auto-recovery-failed" \
            --assignee "${{ github.actor }}" || echo "Could not create issue"

      - name: Upload Deployment Logs and Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vercel-production-deployment-logs
          path: |
            vercel-*.log
            deployment-*.json
            domain-diagnostic-report.json
            domain-verification-report.json
          retention-days: 30